# BUG
A little story I made to start learning C++.
Coding language is C++ (obviously).

If you don't understand, go into code.cpp and read the comments to complete the story. 

run = "clang++-7 -pthread -std=c++17 -o main main.cpp && ./main"
